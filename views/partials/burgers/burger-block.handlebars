<li>
  {{name}}

  <button class="change-devour btn btn-danger" data-id="{{id}}" data-newate="{{ate}}">

    {{#if ate}}DEVOUR ME WHOLE!{{else}}✔️Yum! Back to menu{{/if}}
  </button>

  <button class="delete-burger btn btn-dark" data-id="{{this.id}}">DELETE!</button>
</li>